import { Link } from "react-router-dom";

const UserHistory = () => {
  return (
    <div>
      <button>
        <Link to="/userHistory">UserHistory</Link>
      </button>
    </div>
  );
};

export default UserHistory;
