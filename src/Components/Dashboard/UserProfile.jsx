import { Link } from "react-router-dom";

const UserProfile = () => {
  return (
    <div>
      <button>
        <Link to="/userProfile">UserProfile</Link>
      </button>
    </div>
  );
};

export default UserProfile;
